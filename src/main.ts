/**
 * 应用程序入口
 * 
 * 基于五层双流状态模型的 Blockly Monaco 编辑器
 */

import { createApp } from 'vue'
import App from './App.vue'

const app = createApp(App)

app.mount('#app')

console.log('🚀 Blockly Monaco Editor 启动中...')
console.log('📋 基于五层双流状态模型架构')
